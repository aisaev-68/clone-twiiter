import uvicorn

from settings import PORT
from settings import DEBUG
from settings import HOST

# if __name__ == "__main__":
#     uvicorn.run(
#         app="app:app",
#         host=HOST,
#         port=int(PORT),
#         reload=DEBUG
#     )
